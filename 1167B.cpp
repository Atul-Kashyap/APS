#include<bits/stdc++.h>
using namespace std;
int main()
{
 ios_base::sync_with_stdio(false);
 cin.tie(0);
 cout.tie(0);
 vector< int > v = {4,8,15,16,23,42};
 int a[6],n1,n2;
 cout << "?" << 1 << " " << 1 << endl;
 cin >> n1;
 a[0] = sqrt(n1);
 cout << "?" << 2 << " " << 2 << endl;
 cin >> n1;
 a[1] = sqrt(n1);
 cout << "?" << 3 << " " << 4 << endl;
 cin >> n1;
 cout << "?" << 4 << " " << 5 << endl;
 cin >> n2;
 for(int i = 
return 0;
}