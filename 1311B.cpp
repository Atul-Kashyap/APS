#include<bits/stdc++.h>
using namespace std;
#defin ll long long 
int main()
{
 ll t; cin >> t;
 while(t--)
{
 ll n,m; cin >> n >> m;
 vector<ll> v(n);
 vector<ll> p(m);
 for(auto &a : v) cin >> a;
 for(auto &a : p) cin >> a;
 
 












}
 return 0;
}